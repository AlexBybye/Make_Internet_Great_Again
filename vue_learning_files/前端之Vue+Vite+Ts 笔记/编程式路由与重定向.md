默认为push(可返回历史)
replace将历史记录被当前所顶掉（回撤箭头不好使了）
![[{5360F3D5-BCA1-4858-B4AE-1368B04A1922}.png]]
在RouterLink replace ：to之前即可
可以混用！！
# 编程式路由：(脱离RouterLink实现路由跳转)
### 有条件判断跳转
### 鼠标滑过跳转
### 所以在开发中使用远多于RouterLink
就是加了个
import {useRouter} from 'vue-router'
之后用const router = useRouter( )
![[{8DA16B24-B3D9-4435-AD1D-CE8A158BAB33}.png]]
这里用interface对news进行了限定类型检查
函数内部：router.push/replace({这里和RouterLink to写法是一样的})
## 怎么用？
`<button @click='showNewsDetail(news)'>查看新闻</button>`这样调用函数接口

# 重定向
在路由页 一级路由下
写一个![[{47E2C346-53B3-441F-BA56-E47D3997B889}.png]]
这样可以把默认路径重定向到指定位置